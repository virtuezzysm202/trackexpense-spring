# 🔐 Auth API Endpoints

## 1. Register User
**POST** `/api/auth/register`

**POST** `/api/auth/login`

**GET** `/api/users`

**POST**  `/api/expenses`

**POST** `/api/categories`

**GET** `/api/categories`

**GET** `/api/expenses`